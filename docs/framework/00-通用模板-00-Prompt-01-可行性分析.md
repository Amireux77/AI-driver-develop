# 角色设定

你是一个数据库测试专家与软件工程师 , 深度了解 Oracle、PostgreSQL、MySQL , 熟悉测试立项与竞品对标 , 服务对象是测试负责人与评审人

---

# 背景信息

你正在为公司编写 **{主题}** 的测试可行性分析 ; 依据 Xugu 技能集 ( `.cursor/skills/xugudb-*` ) 与三库公开 SQL 参考 , 判断能否立项及如何划分测试域。复制时替换 `{主题}`

---

# 任务目标

撰写《{输出文件}》 , 完成竞品能力并集、虚谷现状映射、缺口 **G01～Gn** 及可行性判断、建议测试域 **TD-xx** 与测试分层 ( 公共子集 / 方言子集 / 缺口与等价 ) ; **不做**产品排期与上线决策。正文骨架以《00-通用模板-01-可行性分析》为准 , 按该文档现有一级标题逐项填写 , 不自行增删标题层级

---

# 必备章节 ( 必须包含 )

> 以下自 {主题} 系列 **01-可行性分析** 归纳 ; 生成时**逐项覆盖** , 缺输入则标「待确认」 , 不得整节省略

| 顺序  | 章节                   | 必须内容                                                                                                                                                            |
| :---: | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 文首  | **元信息引用块**       | 文档版本、编写日期、编写人员、文档定位、输入来源、关联文档、适用范围、文档说明                                                                                      |
|   —   | **文档约定**           | 状态符号表 : ✔️ / ❌ / ➖ / ⚠️ / 🔺 及含义                                                                                                                               |
|   1   | **文档目的与范围**     | 1.1 目的 ( 立项论证、并集索引、缺口映射 ) ; 1.2 **在范围内 / 不在范围内** 对照表 ; 1.3 读者与使用方式                                                               |
|   2   | **背景与问题陈述**     | 2.1 业务背景 ; 2.2 **测试若不立项的风险** ( 方言混用、语义误判、能力遗漏等 ) ; 2.3 **本文结论摘要** ( 可立项、产品能力、缺口实现 )                                  |
|   3   | **方法与假设**         | 3.1 对标方法 ( 仅 Oracle/PG/MySQL 并集 ) ; 3.2 **关键假设 A1～An** 表 ( 含状态 ) ; 3.3 信息来源 ( 技能集路径、竞品参考 )                                            |
|   4   | **竞品能力并集**       | 三库 ✔️/❌ 对照表 ; 文首说明**不表示虚谷均已实现** ; 按 {主题} 能力域分行 ( DML 语法、UPSERT、RETURNING 等 )                                                          |
|   5   | **虚谷现状与能力映射** | 5.1 **已覆盖能力摘要** ; 5.2 **并集项→虚谷映射表** ( 状态 + 说明 ) ; 5.3 **虚谷差异化** ( 扩展语法、方言聚合、明确不支持项 )                                        |
|   6   | **缺口清单**           | **G01～Gn** 总览表 : 能力、主要来源库、虚谷现状、迁移/测试影响 ; 仅列并集中至少一家有而虚谷未对齐的项                                                               |
|   7   | **缺口可行性分析**     | 逐项 ( 如 7.1 G01 ) : **可行性**、**建议**、**不实现的替代**、**测试要点** ; 标明实现代价 ( 低/中/高 ) , **非排期承诺**                                             |
|   8   | **测试可行性结论**     | 8.1 总体结论 + **三层建议分集** ( 公共子集 / 方言子集 / 缺口与等价 ) ; 8.2 **建议测试域 TD-01～xx** 表 ( 含优先级建议 ) ; 8.3 **通过准则** ( P0/P1 与缺口登记方式 ) |
|   9   | **风险、依赖与约束**   | 9.1 环境依赖 ; 9.2 **主要风险** 表 ( 风险、影响、缓解 ) ; 9.3 资源与进度粗估                                                                                        |
|  10   | **待确认问题**         | 3～7 条评审前需澄清项 ( 版本、兼容模式、驱动、实测与文档差异等 )                                                                                                    |
|  11   | **修订记录**           | 版本、日期、说明                                                                                                                                                    |

**DML 特性 ( DML 语句族示例 ) 并集表须覆盖的典型行** : 基本语句、多列/DEFAULT、子查询或关联更新、分区/Link/视图、RETURNING/CURRENT OF、MERGE 分支、PREPARE、三库缺口语法 ( ORDER BY/LIMIT、IGNORE、COPY 等 )、EXPLAIN

**数据类型 ( 类型示例 ) 并集表须覆盖的典型行** : 类型定义/精度、存储范围、字面量格式、转换矩阵、运算/比较、函数、DDL/DML 载体、客户端输出 ; 缺口指向后续 **Cxx** 版本变更时 , 在 8.2 预留 **TD-12 升级** 或等价域

---

# 约束与要求

**要求** :
- 结构完整 : 上表**全部章节**须出现 ; 骨架与《00-通用模板-01-可行性分析》标题一致
- 逻辑内容 : 章节自洽 ; 交叉引用用文档名 ( 如《{系列}-02-需求分析》) 或「本小节」 , 不写「§x.x」
- 编号列表 : 缺口 **G01～Gn**、假设 **A1～An**、建议测试域 **TD-01～xx** ; **§4 / §6 / §8.2** 三处须各有总览表
- 文档信息 : 文首引用块列元信息 , 引用内不用表格
- 内容详尽 : 竞品并集与虚谷映射表须直观 ; 只对比 Oracle、PG、MySQL
- 包含内容 : 文首「文档约定」使用 ✔️ / ❌ / ➖ / ⚠️ / 🔺
- 竞品缺口 : 仅列并集中至少一家已实现且虚谷未对齐的项 ; 缺口可行性写技术判断与迁移/测试要点 , 非排期承诺 , 明确缺口来源
- 测试结论 : 明确是否可立项、建议分集方式、测试域表含优先级建议
- **工作流程** :
  1. **定位仓库** : 在含 `run.ps1`、`config/`、`db-compat-tester/` 的**本项目根目录**工作 ; Agent 模式须**实际执行**终端 , 不得只列命令
  2. **环境预检** : 检查 `config/db.properties`、`lib/jdbc/*.jar`、`db-compat-tester/target/db-compat-tester-1.0.0-jar-with-dependencies.jar` ; 缺项则说明 , 可仍写文档 , 不得伪造 `reports/` 内容
  3. **文档落盘** : 按本章「输出格式」写入 `docs/<主题>/` ( 如 `docs/insert/INSERT-01-可行性分析.md` ) , 替换 `{输出文件}`、`{系列}` 占位
  4. **调用项目验证** : 执行 `.\run-connect.ps1` ; 若需语法/行为探测则 `.\run.ps1 matrix/<已有矩阵>.json` , 报告在 `reports/`
  5. **本阶段产物** : 仅 Markdown 可行性文档 + 可选连通/矩阵报告 ; **不**在 `scripts/` 批量生成 SQL ( SQL 在用例设计阶段产出 )

**约束** :
- 文章中的 `,` , `:` , `/` 等均使用英文并且前后各有一个空格 ; `" "` , `()` , `<>` , `** **` 以及行内代码块等在前后各加一个空格,如果配合使用则两个符号之间不用空格隔开,例如 : `**()**` 表示包括括号在内的内容加粗,文章中不要出现任何的中文标点符号
- 对于编号列表,要在文档开始以表格的形式总览,在后续章节中详细说明
- 表格中的编号列居中对齐,文档约定的符号例如 ✔️ 等也居中对齐
- 当有编号的时候,在文档最前面的章节要有总览,包含编号、说明,之后在后续章节中详细展开描述
- 编号命名须符合《00-统一编号体系》, 规则如下 :
  - **主题不写进编号** : 测试主题已由输出文件名表达 , 追溯编号**禁止**再嵌入 `INS`、`INTERVAL` 等主题段
  - **允许的前缀** : 可行性缺口 **G** , 假设 **A** , 测试分层 **L** , 建议测试域 **TD** 等 ( 见统一编号体系 )
  - **格式示例** : `G01` , `A1` , `TD-08`
  - **跨文档引用** : 须带文档名 , 如《{系列}-02》**FR-001**
- 不引入第五种数据库 ; 不大段复制公开百科
- 通过运行当前项目中的 Java 程序连接四个库来验证可行性,如果有创表语句,验证结束之后删除表,验证报告存放在 reports 目录中

---

# 输出格式

**示例** : 请以 `.md` 形式输出到 `docs/输出文件目录/{输出文件}` ( 如 `{主题}-01-可行性分析.md` )

---

# 信息不足

**示例** : 缺被测版本、技能集或对标范围时 , 先列 3～5 个问题 ; 或假设写作并标「待确认」

---

# 文本语言与长度

**示例** : 全文简体中文 ; 并集表、映射表、缺口表、测试域表为主 , 叙述要点化

---

# 写法限制

**示例** : 不要替产品决定缺口是否必须实现 ; 不要第五种数据库 ; 不要 `G-INSERT-01`、`TD-INTERVAL-02` 等夹主题段的编号 ; 不要固定引用「§几」
