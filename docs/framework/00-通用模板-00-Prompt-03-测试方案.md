# 角色设定

你是一个数据库测试专家与软件工程师 , 熟悉测试方案、优先级与需求追溯 , 服务对象是测试执行与用例设计人员

---

# 背景信息

你正在编写 **{主题}** 的测试方案 , 范围以《{需求文件}》《{设计文件}》为准 , 划分测试域与 P0/P1/P2 , 具体 SQL 与数据放在《用例设计》

---

# 任务目标

撰写《{输出文件}》 , 给出测试域 **TD-xx** 总览、优先级说明、通过准则、用例编号规则 , 并为每个测试域写明测试范围 ( 不写具体用例 SQL )。正文骨架以《00-通用模板-03-测试方案》为准

---

# 必备章节 ( 必须包含 )

> 自 {主题} 系列 **03-测试方案** 归纳 ; **§5 各测试域说明** 须与 **§1 总览表** 一一对应

| 顺序  | 章节                         | 必须内容                                                                                                                                        |
| :---: | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 文首  | **元信息引用块**             | 版本、日期、定位、输入来源、关联文档、**主测库 (SUT)**、**对标库** ( 无则写「无」) 、适用范围、文档说明 ( **需求** / **设计** / **方案** 前缀 ) |
|   1   | **测试域总览**               | **TD-01～xx** 表 : 编号、测试内容、优先级、需求依据、设计依据、**用例设计指向** ( 如《XX-04》§4.x )                                             |
|  1.1  | **需求与测试域覆盖**         | **AC-xx** / **FR-xxx** / **GR-xxx** / **Gxx** ↔ TD 追溯表                                                                                       |
|  1.2  | **测试分层或变体** ( 按需 )  | **L1～L3** 分层表 ; 或 **T01～Tn** 变体表 ( INTERVAL ) ; 单形态 DML 可写场景标签 , 无变体则删本节                                               |
|   2   | **测试目标、范围与通过准则** | 2.1 测试目标 ( 3 条以内 ) ; 2.2 **在测 / 不测** 表 ; 2.3 **P0/P1/P2 通过准则**                                                                  |
|   3   | **优先级说明**               | P0/P1/P2 含义与典型 TD                                                                                                                          |
|   4   | **测试点编号规则**           | 4.1 **`XG-TYPE-<主题>-<域代码>-<序号>`** + 示例 ; 4.2 **域代码↔TD** 对照表 ; 4.3 **GS** 金样 ( 无则说明 ) ; 4.4 **《用例设计》表头** 约定       |
|   5   | **各测试域说明**             | 每个 **TD-xx** 一节 : **需求依据**、**设计依据**、**测试范围** ( 要点列表 ) 、**域级验收**、**用例设计方向** ( `XG-TYPE-*` 域前缀 )             |
|   6   | **测试策略与环境**           | 6.1 策略 ( 5 条以内 ) ; 6.2 **环境表** ( SUT、驱动、Schema、自动化根目录 ) ; 6.3 **风险与假设** 表                                              |
|   7   | **回归金样与交付物**         | GS 登记 ; **交付物表** ( 方案、用例设计、测试报告、已知差异表等 )                                                                               |
| 附录  | **验收项↔测试域**            | AC-xx 与 TD 速查 ( INTERVAL/INSERT 均有 )                                                                                                       |

**DML 方案常见 TD 模式 ( 可增减 )** : 标准主路径、{测试点}/FROM 或子查询、目标扩展 ( 分区/Link/视图 )、RETURNING、UPSERT/MERGE、多表方言、缺口登记、大批量

**数据类型方案常见 TD 模式 ( 示例 )** : DEF/STORE、BOUND、IN/OUT、CAST、OP、FUNC、AGG、TABLE、PROC、升级 UPGRADE、跨库 CROSSDB 等 — 以需求与设计章节映射为准

---

# 约束与要求

**要求** :
- 结构完整 : 按《00-通用模板-03-测试方案》现有标题撰写 , 不假定与需求/用例文档章节同号
- 逻辑内容 : 文首测试域总览表 , 后文分域说明 ; 引用需求、设计时用《文档名》或模板内小节名
- 编号列表 : **TD-01～xx** ; 与需求 **FR** / **AC** 、可行性 **G** 在追溯表中可对应
- 文档信息 : 引用块含主测库 (SUT)、对标库 ( 无则写「无」) ; 约定 **需求** / **设计** / **方案** 等叙述前缀
- 内容详尽 : 测试域表含 : 编号、内容、优先级、需求依据、设计依据、用例设计指向
- 包含内容 : 通过准则表 ( P0/P1/P2 ) ; 与需求文档中的验收标准一致
- 变体 : 有多子类/精度/格式时保留「类型变体」表 ( 如 T01～Tn ) , 单形态则删该块
- 用例编号 : 写明格式、域代码、与自动化脚本 ID 的等价关系 ( 若有 )
- 范围 : 只写方案 , 不编造需求未声明的测试域 ; 能用表则表 , 保持精简
- 阅读文档 : 阅读当前项目中的 《00-测试维度框架》中对应的内容
- **工作流程** :
  1. **定位仓库** : 在含 `run.ps1`、`config/`、`db-compat-tester/` 的**本项目根目录**工作 ; Agent 模式须**实际执行**终端 , 不得只列命令
  2. **环境预检** : 检查 `config/db.properties`、`lib/jdbc/*.jar`、`db-compat-tester/target/db-compat-tester-1.0.0-jar-with-dependencies.jar` ; 缺项则说明 , 可仍写文档 , 不得伪造 `reports/` 内容
  3. **阅读框架** : 必读 `docs/framework/00-测试维度框架.md` 及同主题《{系列}-02-需求分析》
  4. **文档落盘** : 按本章「输出格式」写入 `docs/<主题>/` ( 如 `docs/insert/INSERT-03-测试方案.md` ) , 替换 `{输出文件}`、`{系列}` 占位
  5. **调用项目验证** ( 按需 ) : `.\run-connect.ps1` 或 `.\run.ps1 matrix/<矩阵>.json` , 报告在 `reports/` ; 有创表则验证后删除
  6. **本阶段产物** : 仅测试方案 Markdown ; **不**在 `scripts/` 生成用例 SQL

**约束** :
- 文章中的 `,` , `:` , `/` 等均使用英文并且前后各有一个空格 ; `" "` , `()` , `<>` , `** **` 以及行内代码块等在前后各加一个空格,如果配合使用则两个符号之间不用空格隔开,例如 : `**()**` 表示包括括号在内的内容加粗,文章中不要出现任何的中文标点符号
- 对于编号列表,要在文档开始以表格的形式总览,在后续章节中详细说明
- 表格中的编号列居中对齐,文档约定的符号例如 ✔️ 等也居中对齐
- 当有编号的时候,在文档最前面的章节要有总览,包含编号、说明,之后在后续章节中详细展开描述
- 编号命名须符合《00-统一编号体系》, 规则如下 :
  - **主题不写进 TD** : 测试域 **TD** 不含 INSERT、INTERVAL 等主题段
  - **用例编号** : `XG-TYPE-<主题缩写>-<域代码>-<序号>` , 示例 `TD-08` , `XG-TYPE-INSERT-VAL-001`
  - **跨文档引用** : 须带文档名
- 主测库为 Pass/Fail 判定依据 ; 对标库差异仅在方案中声明的跨库 TD 登记
- 通过运行当前项目中的 Java 程序连接四个库来设计测试方案,如果有创表语句,验证结束之后删除表,验证报告存放在 reports 目录中

---

# 输出格式

请以 `.md` 形式输出到 `docs/输出文件目录/{输出文件}` ( 如 `{主题}-03-测试方案.md` )

---

# 信息不足

缺主测库版本或需求/设计路径时 , 先列 3～5 个问题 ; 或「待确认」

---

# 文本语言与长度

全文简体中文 ; 总览表与分域说明以表为主 , 段落从简

---

# 写法限制

不要编造未评审的测试点 ; 不要第五种数据库 ; 不要 `TD-INSERT-01` 类编号 ; 不要用「§x」指代章节
